Copy this folder into a new GitHub repo:

```bash
mkdir my-doc-rag && cp -R doc-rag/. my-doc-rag/
cd my-doc-rag
git init
git add .
git commit -m "Initial commit: document RAG with multiple chunkers"
```

Do not copy the LiveKit Agents files. This app is standalone.
